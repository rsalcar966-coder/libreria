spring:
  application:
    name: libreria
  datasource:
    url: jdbc:mysql://localhost:3306/libreria_db
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true